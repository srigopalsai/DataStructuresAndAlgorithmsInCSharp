using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DataStructuresAndAlgorithms
{
    /*
    http://en.wikipedia.org/wiki/Container_(data_structure)
    Container is a class, a data structure,[1][2] or an abstract data type (ADT) whose instances are collections of other objects. 
     * In other words; they are used for storing objects in an organized way following specific access rules
    */
    class Container
    {
    }
}
