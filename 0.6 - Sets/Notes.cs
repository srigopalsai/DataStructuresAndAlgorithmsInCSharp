using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DataStructuresAndAlgorithms.Sets
{
    /*
    ===========================================================================================================================================================================================
    http://en.wikipedia.org/wiki/Set_(mathematics)

    http://www.youtube.com/watch?v=BDYKQtbYxO0&list=PL049927802D0E55C7
    
    Set:   A collection of distinct objects (referred as elements)
    E.g. Set {1,2,3}, Not a Set {1,1,2,3,3}
    can also referred as {integer grater than 0 and less than 4}
    '|' called as such that
    {x | x is an integer and 0 < x < 4 } 
    Can assign names as A = {1,2,3}
    'E' - represents element of
    '₵' - representas not an element 
    2 E A, 2 is element of A

    ===========================================================================================================================================================================================

    Singleton Set A = {2}
    Empty Set       = Ø
    Infinet Set     = {1,2,3,....}
    Intersection    = ∩
    B is sub set of A i.e. A = {1,2,3} , B = {1,2}
    B <  (rotate u ) A
    
    B is not sub set of A i.e. A = {1,2,3} , B = {4,5}
    B < (rotate u and strike it) A

    A={1,2,3} ∩ (Intersection) B={1,3,5}
    A ∩ B = {1,3}
    Can also write as { X | X e A and x e B}

    A={1,2,3} U (Union) B={1,3,5}
    A U B = {1,2,3,5}
    Can also write as { X | X e A and x e B}

    A={1,2,3} - (Difference) B={1,3,5}
    A - B = {1,2,3,5}
    Can also write as { X | X E A and x e B}
    
    ===========================================================================================================================================================================================
    Binary Relation :

    http://en.wikipedia.org/wiki/Binary_relation

    Total Order :
    http://en.wikipedia.org/wiki/Total_order

    Anti Symmetry   :

    Transitivity    :

    Totality        :

    Order Theory    : http://en.wikipedia.org/wiki/Order_theory

    Ordered Pair    : http://en.wikipedia.org/wiki/Ordered_pair

    ===========================================================================================================================================================================================
    */
    class Notes
    {
    }
}
