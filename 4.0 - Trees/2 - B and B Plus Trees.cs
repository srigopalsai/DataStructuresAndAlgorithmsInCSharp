using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DataStructuresAndAlgorithms._4._0___Trees
{
    /**
    B-tree is a self balanced search tree with multiple keys in every node and more than two children for every node.
    But number of keys in a node and number of children for a node is depend on the order of the B-Tree.

        */
    class B_and_B_Plus_Trees
    {
    }
}
